# 설명 (알고리즘)
1. 배열의 값들을 1개씩 분리
2. 분리된 가장 작은 배열들을 2개씩 합치면서 정렬
3. 1.~2. 를 분리된 높이 만큼 반복

# 안정성
 - True

# 정렬기법
 - 내부정렬

# 복잡도

1. 시간복잡도 :
    - best : O(nlog2(n))
    - avg : O(nlog2(n))
    - worst: O(nlog2(n))

2. 공간복잡도 : 0