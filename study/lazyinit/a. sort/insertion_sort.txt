# 설명 (알고리즘)
1. 정렬이 시작될 index 를 1로 지정
2. index 를 1씩 증가시키며 해당 index 보다 작은 값들과 비교하며 작으면 swap
3. 1.~2.를 n번 반복

# 안정성
 - True

# 정렬기법
 - 내부정렬

# 복잡도

1. 시간복잡도 :
    - best : O(n)
    - avg : O(n^2)
    - worst: O(n^2)

2. 공간복잡도 : 0