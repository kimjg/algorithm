# 설명 (알고리즘)
 - 낮은 자리수부터 비교하여 정렬해 간다는 것을 기본 개념으로 하는 정렬 알고리즘
 - 각 자릿수로 마다 카운팅 정렬을 시행한다.

# point
1. 부동소수점 실수처럼 특수한 비교 연산이 필요한 데이터에는 적용할 수 없음

# 안정성 :
 - True

# 정렬기법
 - 내부정렬

# 복잡도

1. 시간복잡도 : d => 가장 큰 데이터 자릿수
    - best : O(dn)
    - avg : O(dn)
    - worst: O(dn)

2. 공간복잡도 : O(n) = radix + n
